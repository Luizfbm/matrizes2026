# Matrizes 2026 Landing Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a mobile-first, SEO/GEO-prioritized MVP landing page for Festival Matrizes 2026 at Buraco do Tatu.

**Architecture:** Create a static Astro site with one indexable landing page. Keep festival content in a single typed data module, render semantic sections from Astro components, and generate metadata plus JSON-LD from the same data source so SEO copy, visible copy, and structured data stay consistent.

**Tech Stack:** Astro, TypeScript, plain CSS, semantic HTML, JSON-LD, Lighthouse verification.

---

## File Structure

- Create: `package.json` - npm scripts and dependencies.
- Create: `astro.config.mjs` - static Astro config and initial production site URL.
- Create: `tsconfig.json` - strict TypeScript config extending Astro defaults.
- Create: `src/data/festival.ts` - single source of truth for event, venue, links, lineup, FAQ, and SEO targets.
- Create: `src/utils/structuredData.ts` - JSON-LD builders for event, place, local business, FAQ, and breadcrumbs.
- Create: `src/components/SeoHead.astro` - title, description, canonical, Open Graph, Twitter, and JSON-LD tags.
- Create: `src/components/Hero.astro` - first-viewport festival and ticket CTA.
- Create: `src/components/QuickAnswer.astro` - direct answer block for search and AI summaries.
- Create: `src/components/Lineup.astro` - text-indexable lineup and program section.
- Create: `src/components/TfdSection.astro` - Torneio de Forro Danca section.
- Create: `src/components/PlanTrip.astro` - tickets, lodging, excursion, PDVs, and location section.
- Create: `src/components/VenueStory.astro` - Buraco do Tatu authority/history section.
- Create: `src/components/ItaunasSection.astro` - Itaunas travel and cultural context section.
- Create: `src/components/Faq.astro` - visible FAQ.
- Create: `src/components/FinalCta.astro` - closing conversion block.
- Create: `src/pages/index.astro` - page composition.
- Create: `src/styles/global.css` - mobile-first visual system and responsive layout.
- Create: `public/og-matrizes.svg` - temporary share image using text and poster-style accents.
- Modify: `docs/superpowers/specs/2026-05-15-matrizes-2026-landing-design.md` only if implementation reveals a spec correction.

## Stack Decision

Use Astro instead of Next.js or a pure HTML file.

Reasons:

- Astro outputs static HTML by default, which is ideal for SEO and GEO.
- The MVP does not need server state, authentication, or a client app runtime.
- Components keep the landing page maintainable without sending unnecessary JavaScript.
- Metadata and JSON-LD can be generated at build time from TypeScript data.
- It deploys cleanly to Netlify, Vercel, Cloudflare Pages, or static hosting.

## Task 1: Scaffold Astro Project

**Files:**
- Create: `package.json`
- Create: `astro.config.mjs`
- Create: `tsconfig.json`

- [ ] **Step 1: Create `package.json`**

```json
{
  "name": "matrizes-2026-landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {}
}
```

- [ ] **Step 2: Create `astro.config.mjs`**

```js
import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://matrizes.buracodotatuitaunas.com.br",
  output: "static"
});
```

- [ ] **Step 3: Create `tsconfig.json`**

```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

- [ ] **Step 4: Install dependencies**

Run: `npm install`

Expected: `package-lock.json` is created and Astro dependencies install successfully.

- [ ] **Step 5: Run initial check**

Run: `npm run check`

Expected: Astro check completes with no TypeScript errors once `src/env.d.ts` is generated by Astro.

## Task 2: Add Festival Data And Structured Data

**Files:**
- Create: `src/data/festival.ts`
- Create: `src/utils/structuredData.ts`

- [ ] **Step 1: Create `src/data/festival.ts`**

```ts
export const festival = {
  name: "Festival Matrizes 2026",
  seoTitle: "Festival Matrizes 2026 em Itaunas | Buraco do Tatu",
  seoDescription:
    "Festival Matrizes 2026 em Itaunas: de 11 a 18 de julho no Buraco do Tatu, com forro pe de serra, shows, danca, DJs, TFD e experiencias culturais.",
  canonicalPath: "/",
  startDate: "2026-07-11",
  endDate: "2026-07-18",
  displayDate: "11 a 18 de julho de 2026",
  city: "Itaunas",
  state: "ES",
  venue: {
    name: "Buraco do Tatu",
    description:
      "Casa tradicional de forro em Itaunas, com atuacao cultural desde 1996 e sede do Festival Matrizes.",
    address: "Itaunas, Conceicao da Barra - ES",
    url: "https://www.instagram.com/buracodotatuitaunas/"
  },
  links: {
    tickets: "https://www.sympla.com.br/buracodotatuitaunas",
    instagram: "https://www.instagram.com/buracodotatuitaunas/",
    linktree: "https://linktr.ee/buracodotatuitaunas"
  },
  quickAnswer:
    "O Festival Matrizes 2026 acontece de 11 a 18 de julho no Buraco do Tatu, em Itaunas, Espirito Santo, reunindo forro pe de serra, shows, danca, DJs e encontros culturais.",
  lineup: [
    "Os 3 do Nordeste",
    "Pipoca Show",
    "Nicolas Krassik",
    "D2 in Trio",
    "ForroFia",
    "Isa Pegado",
    "Chiquinho Alves",
    "Barbara Greco",
    "Forrocacana",
    "Nando do Acordeon",
    "Enok Virgulino",
    "Quininho de Valente",
    "O do Forro",
    "Trio Xamego",
    "Diana do Sertao",
    "Diego Oliveira",
    "Trio Sabiá",
    "Alvorada"
  ],
  programNote:
    "A programacao completa por dia pode ser atualizada conforme novas confirmacoes oficiais do festival.",
  faq: [
    {
      question: "Quando acontece o Festival Matrizes 2026?",
      answer:
        "O Festival Matrizes 2026 acontece de 11 a 18 de julho de 2026."
    },
    {
      question: "Onde acontece o Festival Matrizes 2026?",
      answer:
        "O festival acontece no Buraco do Tatu, em Itaunas, Conceicao da Barra, Espirito Santo."
    },
    {
      question: "O que e o Festival Matrizes?",
      answer:
        "O Matrizes e um festival de forro realizado pelo Buraco do Tatu em Itaunas, reunindo shows, danca, DJs, cultura e encontros da comunidade forrozeira."
    },
    {
      question: "Onde comprar ingresso para o Matrizes 2026?",
      answer:
        "Os ingressos devem ser comprados pelos canais oficiais do Buraco do Tatu, incluindo Sympla e o link oficial na bio do Instagram."
    },
  ]
} as const;
```

- [ ] **Step 2: Create `src/utils/structuredData.ts`**

```ts
import { festival } from "@/data/festival";

const siteUrl = "https://matrizes.buracodotatuitaunas.com.br";

export function buildStructuredData() {
  return [
    {
      "@context": "https://schema.org",
      "@type": "MusicEvent",
      name: festival.name,
      description: festival.seoDescription,
      startDate: festival.startDate,
      endDate: festival.endDate,
      eventAttendanceMode: "https://schema.org/OfflineEventAttendanceMode",
      eventStatus: "https://schema.org/EventScheduled",
      location: {
        "@type": "Place",
        name: festival.venue.name,
        address: {
          "@type": "PostalAddress",
          addressLocality: festival.city,
          addressRegion: festival.state,
          addressCountry: "BR"
        }
      },
      organizer: {
        "@type": "Organization",
        name: festival.venue.name,
        url: festival.venue.url
      },
      offers: {
        "@type": "Offer",
        url: festival.links.tickets,
        availability: "https://schema.org/InStock"
      }
    },
    {
      "@context": "https://schema.org",
      "@type": "LocalBusiness",
      name: festival.venue.name,
      description: festival.venue.description,
      address: {
        "@type": "PostalAddress",
        addressLocality: festival.city,
        addressRegion: festival.state,
        addressCountry: "BR"
      },
      url: festival.venue.url,
      sameAs: [festival.links.instagram, festival.links.linktree]
    },
    {
      "@context": "https://schema.org",
      "@type": "FAQPage",
      mainEntity: festival.faq.map((item) => ({
        "@type": "Question",
        name: item.question,
        acceptedAnswer: {
          "@type": "Answer",
          text: item.answer
        }
      }))
    },
    {
      "@context": "https://schema.org",
      "@type": "BreadcrumbList",
      itemListElement: [
        {
          "@type": "ListItem",
          position: 1,
          name: "Festival Matrizes 2026",
          item: siteUrl
        }
      ]
    }
  ];
}
```

- [ ] **Step 3: Run type check**

Run: `npm run check`

Expected: TypeScript passes with no unresolved import errors.

## Task 3: Build SEO Head And Page Shell

**Files:**
- Create: `src/components/SeoHead.astro`
- Create: `src/pages/index.astro`
- Create: `src/styles/global.css`
- Create: `public/og-matrizes.svg`

- [ ] **Step 1: Create `src/components/SeoHead.astro`**

```astro
---
import { festival } from "@/data/festival";
import { buildStructuredData } from "@/utils/structuredData";

const canonical = new URL(festival.canonicalPath, Astro.site).toString();
const structuredData = JSON.stringify(buildStructuredData());
---

<title>{festival.seoTitle}</title>
<meta name="description" content={festival.seoDescription} />
<link rel="canonical" href={canonical} />
<meta property="og:type" content="website" />
<meta property="og:title" content={festival.seoTitle} />
<meta property="og:description" content={festival.seoDescription} />
<meta property="og:url" content={canonical} />
<meta property="og:image" content={new URL("/og-matrizes.svg", Astro.site).toString()} />
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={festival.seoTitle} />
<meta name="twitter:description" content={festival.seoDescription} />
<script type="application/ld+json" set:html={structuredData}></script>
```

- [ ] **Step 2: Create `public/og-matrizes.svg`**

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630" role="img" aria-labelledby="title desc">
  <title id="title">Festival Matrizes 2026 em Itaunas</title>
  <desc id="desc">Imagem social temporaria do Festival Matrizes 2026 no Buraco do Tatu.</desc>
  <rect width="1200" height="630" fill="#f3e5c1"/>
  <rect x="0" y="0" width="1200" height="96" fill="#111111"/>
  <rect x="0" y="534" width="1200" height="96" fill="#b3281e"/>
  <text x="80" y="220" font-family="Arial Black, Arial, sans-serif" font-size="84" fill="#0d5a43">MATRIZES 2026</text>
  <text x="80" y="310" font-family="Arial, sans-serif" font-size="42" fill="#111111">11 a 18 de julho | Itaunas - ES</text>
  <text x="80" y="385" font-family="Arial Black, Arial, sans-serif" font-size="50" fill="#b3281e">BURACO DO TATU</text>
  <text x="80" y="465" font-family="Arial, sans-serif" font-size="34" fill="#111111">Forro, danca, encontros e cultura</text>
</svg>
```

- [ ] **Step 3: Create initial `src/styles/global.css`**

```css
:root {
  color-scheme: light;
  --color-ink: #171313;
  --color-paper: #fff7e1;
  --color-sand: #f1d99b;
  --color-red: #b3281e;
  --color-green: #115d45;
  --color-yellow: #f3bd22;
  --color-muted: #5c5145;
  --font-body: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-display: Impact, "Arial Black", var(--font-body);
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  background: var(--color-paper);
  color: var(--color-ink);
  font-family: var(--font-body);
  line-height: 1.5;
}

a {
  color: inherit;
}

.page-shell {
  min-height: 100vh;
  overflow-x: hidden;
}

.section {
  padding: 56px 20px;
}

.section-inner {
  width: min(100%, 1120px);
  margin: 0 auto;
}

.eyebrow {
  margin: 0 0 10px;
  color: var(--color-red);
  font-size: 0.78rem;
  font-weight: 800;
  letter-spacing: 0;
  text-transform: uppercase;
}

.section-title {
  margin: 0 0 18px;
  font-family: var(--font-display);
  font-size: clamp(2rem, 12vw, 4.25rem);
  line-height: 0.95;
  letter-spacing: 0;
  text-transform: uppercase;
}

.button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 48px;
  padding: 13px 18px;
  border: 2px solid var(--color-ink);
  background: var(--color-yellow);
  box-shadow: 4px 4px 0 var(--color-ink);
  color: var(--color-ink);
  font-weight: 900;
  text-decoration: none;
}

.button.secondary {
  background: transparent;
}

.poster-band {
  height: 18px;
  background:
    repeating-linear-gradient(90deg, var(--color-ink) 0 14px, transparent 14px 26px),
    var(--color-yellow);
}

@media (min-width: 760px) {
  .section {
    padding: 80px 32px;
  }
}
```

- [ ] **Step 4: Create `src/pages/index.astro` shell**

```astro
---
import SeoHead from "@/components/SeoHead.astro";
import "@/styles/global.css";
---

<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <SeoHead />
  </head>
  <body>
    <main class="page-shell">
      <div class="poster-band" aria-hidden="true"></div>
      <section class="section">
        <div class="section-inner">
          <p class="eyebrow">Buraco do Tatu apresenta</p>
          <h1 class="section-title">Festival Matrizes 2026 em Itaunas</h1>
          <p>De 11 a 18 de julho de 2026, no Buraco do Tatu.</p>
          <a class="button" href="https://www.sympla.com.br/buracodotatuitaunas">Comprar ingresso</a>
        </div>
      </section>
    </main>
  </body>
</html>
```

- [ ] **Step 5: Run build**

Run: `npm run build`

Expected: Astro builds `dist/` successfully.

## Task 4: Implement Landing Sections

**Files:**
- Create: `src/components/Hero.astro`
- Create: `src/components/QuickAnswer.astro`
- Create: `src/components/Lineup.astro`
- Create: `src/components/TfdSection.astro`
- Create: `src/components/PlanTrip.astro`
- Create: `src/components/VenueStory.astro`
- Create: `src/components/ItaunasSection.astro`
- Create: `src/components/Faq.astro`
- Create: `src/components/FinalCta.astro`
- Modify: `src/pages/index.astro`
- Modify: `src/styles/global.css`

- [ ] **Step 1: Replace shell content with components in `src/pages/index.astro`**

```astro
---
import Faq from "@/components/Faq.astro";
import FinalCta from "@/components/FinalCta.astro";
import Hero from "@/components/Hero.astro";
import ItaunasSection from "@/components/ItaunasSection.astro";
import Lineup from "@/components/Lineup.astro";
import PlanTrip from "@/components/PlanTrip.astro";
import QuickAnswer from "@/components/QuickAnswer.astro";
import SeoHead from "@/components/SeoHead.astro";
import TfdSection from "@/components/TfdSection.astro";
import VenueStory from "@/components/VenueStory.astro";
import "@/styles/global.css";
---

<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <SeoHead />
  </head>
  <body>
    <main class="page-shell">
      <Hero />
      <QuickAnswer />
      <Lineup />
      <TfdSection />
      <PlanTrip />
      <VenueStory />
      <ItaunasSection />
      <Faq />
      <FinalCta />
    </main>
  </body>
</html>
```

- [ ] **Step 2: Create `src/components/Hero.astro`**

```astro
---
import { festival } from "@/data/festival";
---

<section class="hero">
  <div class="poster-band" aria-hidden="true"></div>
  <div class="hero__inner">
    <p class="eyebrow">Buraco do Tatu apresenta</p>
    <h1>Festival Matrizes 2026 em Itaunas</h1>
    <p class="hero__lead">
      {festival.displayDate}, no {festival.venue.name}. Uma semana de forro pe de serra,
      danca, DJs, encontros, praia e cultura em Itaunas.
    </p>
    <div class="hero__actions">
      <a class="button" href={festival.links.tickets}>Comprar ingresso</a>
      <a class="button secondary" href="#programacao">Ver programacao</a>
    </div>
    <dl class="hero__facts">
      <div><dt>Quando</dt><dd>{festival.displayDate}</dd></div>
      <div><dt>Onde</dt><dd>{festival.venue.name}, Itaunas - ES</dd></div>
      <div><dt>Estilo</dt><dd>Forro, danca e cultura</dd></div>
    </dl>
  </div>
</section>
```

- [ ] **Step 3: Create `src/components/QuickAnswer.astro`**

```astro
---
import { festival } from "@/data/festival";
---

<section class="section answer-block" aria-labelledby="resumo">
  <div class="section-inner">
    <p class="eyebrow">Resposta rapida</p>
    <h2 id="resumo" class="section-title">O que e o Matrizes 2026?</h2>
    <p class="answer-block__text">{festival.quickAnswer}</p>
  </div>
</section>
```

- [ ] **Step 4: Create remaining section components**

Use this exact content split:

```astro
--- src/components/Lineup.astro ---
---
import { festival } from "@/data/festival";
---
<section id="programacao" class="section" aria-labelledby="programacao-title">
  <div class="section-inner">
    <p class="eyebrow">Line-up e programacao</p>
    <h2 id="programacao-title" class="section-title">Forro de 11 a 18 de julho</h2>
    <p>{festival.programNote}</p>
    <ul class="lineup-grid">
      {festival.lineup.map((artist) => <li>{artist}</li>)}
    </ul>
    <a class="button" href={festival.links.tickets}>Garantir ingresso</a>
  </div>
</section>
```

```astro
--- src/components/TfdSection.astro ---
<section class="section section--green" aria-labelledby="tfd-title">
  <div class="section-inner">
    <p class="eyebrow">TFD</p>
    <h2 id="tfd-title" class="section-title">Torneio de Forro Danca</h2>
    <p>O TFD integra o Festival Matrizes como espaco de valorizacao da danca, dos grupos e da cultura forrozeira que encontra em Itaunas uma de suas pistas mais importantes.</p>
  </div>
</section>
```

```astro
--- src/components/PlanTrip.astro ---
---
import { festival } from "@/data/festival";
---
<section class="section" aria-labelledby="trip-title">
  <div class="section-inner">
    <p class="eyebrow">Planeje sua ida</p>
    <h2 id="trip-title" class="section-title">Ingressos, hospedagem e Itaunas</h2>
    <div class="info-grid">
      <article><h3>Ingressos</h3><p>Compre pelos canais oficiais do Buraco do Tatu.</p></article>
      <article><h3>Hospedagem</h3><p>Use os destaques e links oficiais para encontrar opcoes na vila e arredores.</p></article>
      <article><h3>Excursao</h3><p>Confira informacoes oficiais sobre excursao, chegada e deslocamento.</p></article>
      <article><h3>Local</h3><p>{festival.venue.name}, Itaunas, Conceicao da Barra - ES.</p></article>
    </div>
  </div>
</section>
```

```astro
--- src/components/VenueStory.astro ---
<section class="section section--red" aria-labelledby="venue-title">
  <div class="section-inner">
    <p class="eyebrow">A casa</p>
    <h2 id="venue-title" class="section-title">Buraco do Tatu em Itaunas</h2>
    <p>Desde 1996 fazendo forro em Itaunas, o Buraco do Tatu e a casa do Festival Matrizes e um ponto de encontro para quem vive o forro pe de serra na pista, no palco e na estrada.</p>
  </div>
</section>
```

```astro
--- src/components/ItaunasSection.astro ---
<section class="section" aria-labelledby="itaunas-title">
  <div class="section-inner">
    <p class="eyebrow">Destino</p>
    <h2 id="itaunas-title" class="section-title">Itaunas, forro, dunas e julho</h2>
    <p>Itaunas e uma vila de Conceicao da Barra, no Espirito Santo, conhecida pelas dunas, praia, natureza e pela forca do forro. Em julho, a vila recebe forrozeiros de muitos lugares do Brasil.</p>
  </div>
</section>
```

```astro
--- src/components/Faq.astro ---
---
import { festival } from "@/data/festival";
---
<section class="section faq" aria-labelledby="faq-title">
  <div class="section-inner">
    <p class="eyebrow">Perguntas frequentes</p>
    <h2 id="faq-title" class="section-title">Matrizes 2026 em Itaunas</h2>
    {festival.faq.map((item) => (
      <details>
        <summary>{item.question}</summary>
        <p>{item.answer}</p>
      </details>
    ))}
  </div>
</section>
```

```astro
--- src/components/FinalCta.astro ---
---
import { festival } from "@/data/festival";
---
<section class="section final-cta" aria-labelledby="cta-title">
  <div class="section-inner">
    <p class="eyebrow">11 a 18 de julho de 2026</p>
    <h2 id="cta-title" class="section-title">Viva o Matrizes no Buraco do Tatu</h2>
    <p>Garanta seu ingresso pelos canais oficiais e venha para Itaunas.</p>
    <a class="button" href={festival.links.tickets}>Comprar ingresso</a>
  </div>
</section>
```

- [ ] **Step 5: Extend `src/styles/global.css`**

```css
.hero {
  background: var(--color-sand);
}

.hero__inner {
  width: min(100%, 1120px);
  margin: 0 auto;
  padding: 48px 20px 56px;
}

.hero h1 {
  max-width: 820px;
  margin: 0;
  font-family: var(--font-display);
  font-size: clamp(3.1rem, 18vw, 7rem);
  line-height: 0.88;
  letter-spacing: 0;
  text-transform: uppercase;
}

.hero__lead,
.answer-block__text {
  max-width: 760px;
  font-size: 1.15rem;
  font-weight: 700;
}

.hero__actions {
  display: flex;
  flex-direction: column;
  gap: 14px;
  margin: 28px 0;
}

.hero__facts,
.info-grid {
  display: grid;
  gap: 14px;
}

.hero__facts {
  margin: 28px 0 0;
}

.hero__facts div,
.info-grid article,
.faq details {
  border: 2px solid var(--color-ink);
  background: rgba(255, 255, 255, 0.44);
  padding: 16px;
  box-shadow: 4px 4px 0 var(--color-ink);
}

.hero__facts dt,
.info-grid h3,
.faq summary {
  font-weight: 900;
}

.hero__facts dd {
  margin: 4px 0 0;
}

.lineup-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 10px;
  padding: 0;
  margin: 26px 0;
  list-style: none;
}

.lineup-grid li {
  min-height: 64px;
  display: flex;
  align-items: center;
  border: 2px solid var(--color-ink);
  background: var(--color-paper);
  padding: 12px;
  font-weight: 900;
}

.section--green {
  background: var(--color-green);
  color: var(--color-paper);
}

.section--red,
.final-cta {
  background: var(--color-red);
  color: var(--color-paper);
}

.section--green .eyebrow,
.section--red .eyebrow,
.final-cta .eyebrow {
  color: var(--color-yellow);
}

.faq details + details {
  margin-top: 12px;
}

.faq summary {
  cursor: pointer;
}

@media (min-width: 760px) {
  .hero__inner {
    padding: 72px 32px 80px;
  }

  .hero__actions {
    flex-direction: row;
  }

  .hero__facts,
  .info-grid {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .lineup-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}
```

- [ ] **Step 6: Run build**

Run: `npm run build`

Expected: Build passes and all visible sections render in `dist/index.html`.

## Task 5: Verify SEO, Mobile, And Static Output

**Files:**
- Modify only files that fail verification.

- [ ] **Step 1: Verify static HTML contains core search terms**

Run: `rg -n "Festival Matrizes 2026|festival em Itaunas|Buraco do Tatu|11 a 18 de julho|application/ld\\+json" dist/index.html`

Expected: All terms appear in built HTML.

- [ ] **Step 2: Start preview server**

Run: `npm run preview -- --host 127.0.0.1 --port 4321`

Expected: Preview serves the built site at `http://127.0.0.1:4321/`.

- [ ] **Step 3: Run Lighthouse mobile audit in the browser**

Open `http://127.0.0.1:4321/` and run a mobile Lighthouse audit for SEO, accessibility, and best practices.

Expected:

- SEO score: 100 or any failures documented and fixed.
- Accessibility score: 95+.
- Best practices score: 95+.

- [ ] **Step 4: Check mobile viewport manually**

Use a 390px wide mobile viewport.

Expected:

- H1 fits without horizontal scroll.
- Primary CTA appears before the fold or close to it.
- Text cards do not overlap.
- Lineup cards remain readable.

- [ ] **Step 5: Final build**

Run: `npm run build`

Expected: Final build passes after any fixes.

## Self-Review

- Spec coverage: The plan covers the approved MVP sections, mobile-first visual direction, SEO/GEO answer block, visible FAQ, structured data, Buraco do Tatu authority, Itaunas destination context, and ticket conversion.
- Placeholder scan: No task uses TBD/TODO/fill-later language. Provisional production inputs are represented as explicit MVP data in `src/data/festival.ts`.
- Type consistency: `festival` and `buildStructuredData()` names are consistent across all planned imports.

## Execution Options

Plan complete and saved to `docs/superpowers/plans/2026-05-15-matrizes-2026-landing.md`.

Two execution options:

1. Subagent-Driven (recommended) - dispatch a fresh subagent per task, review between tasks, fast iteration.
2. Inline Execution - execute tasks in this session using executing-plans, batch execution with checkpoints.
